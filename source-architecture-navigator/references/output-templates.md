---
name: output-templates
description: 源码架构导航输出模板，包括问题分类卡、项目识别卡、函数地图、调用链、体检报告和施工边界卡。
version: 1.0.0
---

# Output Templates

<!-- @类型: 模板参考 -->
<!-- @目的: 保持源码阅读输出结构稳定，方便多轮导航和复盘 -->

## 1. 问题分类卡

```markdown
问题类别: 理解类 / 定位类 / 调用链类 / 架构类 / bug 类 / 需求类 / 重构类 / 施工类 / 复盘类
当前模式: 读模式 / 施工前边界 / 施工模式
本轮目标:
不做什么:
需要证据:
```

## 2. 项目识别卡

```markdown
项目目标:
技术栈:
入口候选:
核心路径:
边缘路径:
配置来源:
运行/测试入口:
不确定项:
```

## 3. 仓库/zip 首轮导航模板

````markdown
问题类别: 架构类 / 理解类 / 定位类
当前边界: 只读；zip 输入只解到临时目录或用户指定分析目录

L0 项目识别卡:
- 项目目标:
- 入口候选:
- 核心路径:
- 边缘路径:
- 配置来源:
- 不确定项:

L1 核心对象小图:
- 对象:
- 位置:
- 输入/输出:
- 副作用:
- 本轮不适用原因: 如用户未指定对象，可写"需下一问指定函数/字段"

L2 调用链小图:
```mermaid
flowchart LR
  A["入口"] --> B["核心编排"]
  B --> C["核心模块"]
```

L3 功能/数据/配置流:
```mermaid
flowchart TD
  A["输入"] --> B["转换"]
  B --> C["输出"]
```

证据表:
| 对象/边/路线项 | 证据等级 | 证据位置 | 状态 |
|---|---|---|---|
| 入口候选 | E3 | `zip/path/or/repo/path:line` | 已证实 / 推断 |

建议先看 3-5 个文件:
1. `path/to/file.ext`
   读什么:
   解决什么:
   暂时跳过:

下一问:
- 请只追踪 <函数/字段/接口>，给我 L1 地图和上下游。
````

## 4. 函数地图模板

````markdown
函数:
位置:
一句话职责:
输入:
输出:
副作用:
上游调用:
下游依赖:
容易误解:

地图:
```mermaid
flowchart TD
  A["输入"] --> B["校验/转换"]
  B --> C["核心计算"]
  C --> D["输出"]
  C --> E["副作用"]
```

证据表:
| 节点/边 | 证据等级 | 证据位置 | 状态 |
|---|---|---|---|
| 函数定义 | E1 | `path/to/file.ext:line` | 已证实 |

读图顺序:
下一张图:
````

## 5. 调用链模板

````markdown
追踪对象:
定义点:
上游:
当前节点:
下游:
终点:
证据:

```mermaid
flowchart LR
  U1["上游调用者"] --> T["当前对象"]
  T --> D1["核心依赖"]
  D1 --> O1["输出/终点"]
```

证据表:
| 边 | 证据等级 | 证据位置 | 状态 |
|---|---|---|---|
| 上游 -> 当前对象 | E2 | `path/to/file.ext:line` | 已证实 / 推断 |
````

## 6. 字段旅程模板

````markdown
字段:
诞生位置:
初始含义:
转换位置:
存储位置:
消费位置:
展示/返回位置:
一致性风险:
证据:

```mermaid
flowchart LR
  A["诞生"] --> B["转换"]
  B --> C["存储/缓存"]
  C --> D["消费"]
  D --> E["展示/返回"]
```

证据表:
| 阶段 | 证据等级 | 证据位置 | 状态 |
|---|---|---|---|
| 诞生 | E1 | `path/to/file.ext:line` | 已证实 |
````

## 7. 架构体检报告模板

```markdown
范围:
已检查证据:
总体判断:

发现 1:
严重度:
证据:
影响:
最小下一步:
验收方式:
回滚思路:

发现 2:
...

稳定性判断:
建议阅读路线:
下一问:
```

严重度建议：

- P0: 当前功能可能错误或不可运行。
- P1: 高概率造成后续修改不一致或难以验证。
- P2: 明显增加理解成本，但暂不阻断功能。
- P3: 风格或局部可读性问题。

## 8. 阅读路线模板

```markdown
建议先看 3-5 个文件:
1. path/to/file.ext
   读什么:
   解决什么:
   暂时跳过:
   证据: E1/E2/E3 `path/to/file.ext:line`
2. path/to/next.ext
   读什么:
   解决什么:
   暂时跳过:
   证据: E1/E2/E3 `path/to/next.ext:line`

看完后的检查问题:
- 我现在知道入口在哪里吗？
- 我知道核心数据结构从哪里来、到哪里去吗？
- 我知道下一层要追哪个函数/字段吗？
```

## 9. 施工边界卡

```markdown
问题类别:
目标:
涉及文件:
不涉及文件:
修改边界:
主要风险:
验收方式:
回滚方式:
```

## 10. 一次全解析 HTML 报告内容清单

```markdown
报告标题:
输入来源:
输出位置:
项目识别卡:
解析覆盖矩阵:
L3 全局分层架构:
L2 模块关系图:
L1 逐函数地图:
Golden Path 核心路径:
配置依赖检查:
接口契约候选:
问题诊断:
新手阅读路线:
高手阅读路线:
证据表:
下一问:
交互: 函数筛选 / 展开收起 / 主路径高亮 / 证据表显隐 / 任意点击正文写笔记 / 划选原文写笔记
笔记状态: 点击后下沉并打开输入；无输入恢复；保存后凸起标记已有笔记
笔记导出:
- 按照顺序：<用户记录内容>
- 原文：<选中文本或位置>
  记录：<用户记录内容>
```

HTML 报告是首轮源码阅读的主产物。默认放在被扫描仓库外；聊天回答中给报告路径和 3-5 行摘要，不再重复整份报告。

## 11. 下一问模板

下钻：

```markdown
请只追踪 <函数/字段/组件/接口>，给我 L1 函数地图和它的上下游。
```

横向：

```markdown
请比较 <模块A> 和 <模块B> 是否在做重复的事，只给有证据的结论。
```

进入施工前：

```markdown
基于刚才的理解，把 <目标> 转成施工边界卡，先不要改代码。
```

复盘：

```markdown
把我们刚才问过的问题整理成问题树：已解决、未解决、下一步。
```

## 版本历史

- **v1.0.0** (2026-05-27) - 初始版本，提供核心输出模板。
